"""The toolbox.

Split on purpose. The data detective can query the warehouse and cannot open a
file. The lineage detective can open files and cannot query the warehouse.

An agent with every tool will use every tool.
"""
