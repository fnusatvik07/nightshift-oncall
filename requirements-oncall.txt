# The two services. Everything the course itself needs is in requirements.txt.

# ── the signal API and the agent's HTTP surface ───────────────────────────
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
pydantic>=2.7,<3

# ── the agent system ──────────────────────────────────────────────────────
# LangChain 1.x. The multi agent pattern here is subagents as tools, which is
# what replaced the deprecated langgraph-supervisor package.
langchain>=1.0,<2
langchain-openai>=1.0,<2
langgraph>=1.0,<2
